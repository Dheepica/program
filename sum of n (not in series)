#include<conio.h>
#include<stdio.h>
int main()
{
    int i,sum=0,n,array[20];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&array[i]);
        sum=sum+array[i];
    }
    printf("%d",sum);
    return 0;
}
